# Minimal dependencies for running the test suite in CI (unit + integration).
# Install with: pip install -r requirements-test.txt
#
# Full production dependencies (requirements.txt) are NOT needed for the
# unit tests; heavy packages (weasyprint, pymupdf, pgvector, …) are skipped
# automatically via pytest.importorskip() in the relevant test files.

pytest>=7.4
pytest-flask>=1.3

# Core runtime — needed by most test files.
Flask>=3.0
python-dotenv>=1.0
dropbox>=11.36
bcrypt>=4.0
Flask-Limiter>=3.5
Flask-Babel>=4.0

# PrionVault routes (test_validate_notif_payload).
SQLAlchemy>=2.0
